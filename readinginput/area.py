# calculate the area of a rectangle by entering length and width
# calculate the area and display the results

Length = int(input("length of the rectangle:"))
Width = int(input("Width of the rectangle:"))
Area = Length * Width
print(Area)

